// Advance Types in TS:
// Generic --> Is way of type annotation
// can type annotation --> mutipal places
// moto : flexible, reusable

function get(para: number): number {
  return para;
}
console.log(get(5));

function get2(para: string): string {
  return para;
}
console.log(get2("JS"));

// diff function defination ?
